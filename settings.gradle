dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)  // ← 改这里
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "NahidaGift"
include ':app'
